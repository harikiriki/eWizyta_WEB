// ChatModel.js
export default class Chat {
    constructor(id, name, lastMessage, timestamp) {
        this.id = id;
        this.name = name;
        this.lastMessage = lastMessage;
        this.timestamp = timestamp;
    }
}
